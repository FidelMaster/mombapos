<% content_for :title, "Editar Usuario" %>

<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-black text-slate-900 tracking-tight">Editar Usuario</h1>
    <p class="text-sm font-medium text-slate-500 mt-1">Actualiza los datos y el rol de <%= @user.email %>.</p>
  </div>

  <%= render "form", user: @user %>
</div>
