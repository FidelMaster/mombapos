<% content_for :title, "Editar Rol" %>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-black text-slate-900 tracking-tight">Editar Rol</h1>
    <p class="text-sm font-medium text-slate-500 mt-1">Configura el nombre y los permisos de acceso para <%= @role.name %>.</p>
  </div>

  <%= render "form", role: @role %>
</div>
